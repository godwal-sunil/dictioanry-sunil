<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">

    <script src="https://kit.fontawesome.com/610955e538.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Output</title>
    <style>


    body {
      background-color: #B1D7B4;
      padding-bottom:30px; }
    .output-section{
      background-color: #84a78c78;
      margin-top: 30px;
      border: 2px solid #90C8AC;
      border-radius: 10px;
      padding: 35px 20px;
      height: auto;
      z-index: 4px;
      box-shadow: 5px 10px 20px #42644ae8 inset;
    }
    .output-section:hover{
      border-color: #42644ae8;
      box-shadow: 1px 1px 8px 10px black;
      box-shadow: 5px 10px 20px #42644ae8 inset;
    }
    .container{
      width:50%; margin:auto;margin-top: 50px
    }
    .heading {
      color: #647E68;
    }
    .headline {
      color: #1C6758;
      margin-bottom: 10px;
    }
    .result {
      font-size: 18px;
    }
    .title{
      text-align:center;
      color: #ffffff;
    }
    .resetBtn {
      font-family: cursive;
      position: relative;
      left: 160px;
      width: 50%;
      margin: auto;
      align-items: center;
      background-color: #294e32c4;
      font-size: 20px;
      color: #FFFFFF;
      padding: 9px ;
      transform: translateY(0%);
      border-radius: 12px;
      border: 1px solid;
      padding: 10px;
      box-shadow: 5px 10px 20px #bbbbbb inset;

    }
    .resetBtn:hover {
    background-color:#739f7ec4;
    color: black;
    border-color: #ffffff;
    box-shadow: 1px 1px 5px 8px #90C8AC;
}

    </style>
  </head>
  <body >

    <div class="container">
      <div class="output-section">
        <h1 class="title" >English Dictionary</h1>
        <h1 class="heading headline" >You searched: <% if (data) { %>
                <%= data.word %>
                <%= data.phonetic %>
        </h1>

        <table class="table table-striped result">
          <% if (data.meanings.length> 0) { %>
          <% for(var i=0; i < data.meanings.length; i++) { %>
      <tr>
          <td>Meaning: </td>
          <td><%= data.meanings[i].definitions[0].definition %></td>
      </tr>
        <tr>
            <td>Example:
            </td>
            <td>
                <%= data.meanings[i].definitions[0].example %>
            </td>
        </tr>
    <% } %>
   <% } %>
    </table>
    <% } %>
        <form class="" action="/reset" method="post">
          <button class="resetBtn" type="submit" name="button" >Reset</button>
        </form>
      </div>

    </div>


  </body>
</html>
